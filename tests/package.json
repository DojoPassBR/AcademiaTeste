{
  "name": "academiateste-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cd .. && ./tests/node_modules/.bin/firebase emulators:exec --project academiateste-tests --only firestore \"./tests/node_modules/.bin/vitest run --root tests --no-file-parallelism\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^13.0.0",
    "vitest": "^2.0.0"
  }
}
